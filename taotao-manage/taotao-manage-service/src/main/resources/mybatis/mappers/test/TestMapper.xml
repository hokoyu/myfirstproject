<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hekeyu.taotao.mapper.TestMapper">
	<!-- 获取当前时间 -->
	<select id="queryCurrentDate" resultType="string">
		select now()
	</select>
</mapper>